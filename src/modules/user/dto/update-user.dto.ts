/**
 * Re-exported for backward compatibility with AuthService.
 * New code should use UpdateProfileDto (own profile) or AdminUpdateUserDto (admin edits).
 */
export { UpdateProfileDto as UpdateUserDto } from './update-profile.dto';
